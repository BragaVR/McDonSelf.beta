let products = [
]; 

//fare parte per salvarela quantità dell'ordine e fare la parte che permette di ordinare più cose assieme

const MacchinaSelf = {
	data() {
		return {
			count: 1
			if (quantità) {
				return true
				quantità ++;

			}else
		}
	} 

};

Vue.createApp(MacchinaSelf).mount('#app');










 /*  

<div v-for="quantità.quantità-ordine">
        {{quantità.quantità}}
        <div v-if="quantità-ordine">
          <div v-for="quantità">
            
        
        {{item += 1}}
      </div>

*/